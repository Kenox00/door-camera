# ==========================================
# CAMERA APP CONFIGURATION
# ==========================================

# API Configuration
# Backend REST API URL (without /api suffix)
VITE_API_URL=http://localhost:3001/api

# WebSocket Configuration
# Backend WebSocket URL (for real-time communication)
VITE_WS_URL=http://localhost:3001

# Device Configuration
# Device ID (must be registered in backend via POST /api/devices/register)
# Get this from your backend admin panel or API response
VITE_DEVICE_ID=camera-001

# Device Name (optional, for display purposes)
VITE_DEVICE_NAME=ESP32-CAM Door Camera

# Device Location (optional)
VITE_DEVICE_LOCATION=Front Door

# WebSocket Reconnection Settings
VITE_WS_RECONNECTION_ATTEMPTS=5
VITE_WS_RECONNECTION_DELAY=1000

# Environment
VITE_ENV=development

# Optional: JWT Token (set after authentication)
# Leave empty if using dynamic authentication
# VITE_JWT_TOKEN=your_jwt_token_here
